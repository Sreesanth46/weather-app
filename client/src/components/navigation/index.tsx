import { CommandSearch } from "../command-diaglogue";

const Navigation = () => {
  return (
    <nav className="flex w-full items-center justify-between py-4">
      <div />
      <CommandSearch />
    </nav>
  );
};

export default Navigation;
